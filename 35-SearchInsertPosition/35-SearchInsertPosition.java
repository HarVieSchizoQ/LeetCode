                return mid;

            if (nums[mid] > target) {
                high = mid - 1;
            } else {
            int mid = (low + high) / 2;

            if (nums[mid] == target)
        
        while (low <= high) {
        // 0 (log n)
        int low = 0, high = nums.length - 1;
[
