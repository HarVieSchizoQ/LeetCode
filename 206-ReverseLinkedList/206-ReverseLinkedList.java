            current.next = prev;
            current = next;
        }

        head = prev;
        return head;
     }        
        while (current != null) { 
            ListNode next = current.next;
        ListNode prev = null;

            prev = current; 
[
