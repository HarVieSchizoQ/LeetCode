            }
        }
                if (isPalindrome(current)) 
                    res = current.length() > res.length() ? current: res;
            for (int j = i; j < s.length(); j++) {
                current = s.substring(i, j + 1);
                

        return res;
    }

    public boolean isPalindrome(String current) {
        int low = 0, high = current.length() - 1;
                    
        while (low < high) {
             if (current.charAt(low) != current.charAt(high)) 
"
